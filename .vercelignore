# Excluded from the Vercel deploy (still kept in git).
# The 3 root PDPs need amin/ (46-58 refs each) -> amin/ MUST ship.
# assets/ is referenced 0 times by the live PDPs, so it is excluded (saves ~144MB).
assets/
tweaks-panel.jsx
